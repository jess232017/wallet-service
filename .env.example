# Example environment configuration for wallet-service
# Copy this file to .env and adjust values as needed.
# WARNING: Never use DB_SYNC=true in production!

# Database host (e.g., localhost or your DB server address)
DB_HOST=localhost

# Database port (default for PostgreSQL is 5432)
DB_PORT=5432

# Database username
DB_USER=postgres

# Database password
DB_PASS=root

# Database name
DB_NAME=wallet_db

# TypeORM: synchronize schema on every app launch (use 'true' ONLY in development)
DB_SYNC=false

# TypeORM: enable SQL query logging (set to 'true' for debugging)
DB_LOGGING=false